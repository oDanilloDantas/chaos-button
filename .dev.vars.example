# Local Worker secrets. Copy this file to `.dev.vars` and fill in real values.
# `.dev.vars` is git-ignored and must never be committed.
# In production these are set with: wrangler secret put <NAME>

N8N_CAPTURE_URL="https://example.com/webhook/your-webhook-id"
